# Step 1: Use an official Python image as base
FROM python:3.9-slim

# Step 2: Set working directory in container
WORKDIR /app

# Step 3: Copy all project files into the container
COPY . .

# Step 4: Install required Python packages
RUN pip install --no-cache-dir -r requirements.txt

# Step 5: Expose port (Flask usually runs on 5000)
EXPOSE 5000

# Step 6: Define the command to run the app
CMD ["python", "app.py"]

